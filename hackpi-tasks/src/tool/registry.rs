// ── Tool registry ───────────────────────────────────────────────────────────
//
// This module will contain tool registry, discovery, and listing utilities.
// Currently, the `register` method is defined directly on `TaskTool` in `mod.rs`.
