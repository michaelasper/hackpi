//! Config validation rules and cross-field validation.
//!
//! This module is reserved for config validation logic extracted from
//! the config system. The primary validation function (`validate`)
//! currently lives in `crate::hot_reload` and will be migrated here
//! in a future change.
